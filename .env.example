# Environment (safe to commit)

# Public RPC / indexer — no keys required
VITE_RPC_URL=https://api.infra.testnet.somnia.network
VITE_RPC_FALLBACK_URL=https://dream-rpc.somnia.network
VITE_WS_RPC_URL=wss://api.infra.testnet.somnia.network/ws
VITE_INDEXER_URL=https://dev.smk.somnia.host/v1/graphql

# Optional: pin venue. If empty, Window reads venueId from the first live binary row.
VITE_VENUE_ID=
